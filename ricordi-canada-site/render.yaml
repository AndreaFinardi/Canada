services:
  - type: web
    name: ricordi-canada
    runtime: node
    plan: free
    buildCommand: npm ci --no-audit --no-fund
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: ACCESS_CODE
        sync: false
      - key: SESSION_SECRET
        generateValue: true
